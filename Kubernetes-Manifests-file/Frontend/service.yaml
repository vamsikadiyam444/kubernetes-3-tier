apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: three-tier
  labels:
    role: frontend
spec:
  type: LoadBalancer
  selector:
    role: frontend
  ports:
    - port: 80
      targetPort: 80  # ← Change this from 3000 to 80
